# Copy to deploy/.env on the server and fill in. Generate secrets with: openssl rand -hex 32
DOMAIN=artiqusurface.com
DB_ROOT_PASSWORD=
REDIS_PASSWORD=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_RESET_SECRET=
# optional (password-reset emails)
SMTP_HOST=smtp.gmail.com
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
